{
  "name": "jaminst-ui-app",
  "version": "0.1.0",
  "description": "> TODO: description",
  "keywords": [],
  "license": "MIT",
  "private": "true",
  "main": "lib/index.js",
  "module": "lib-es/index.js",
  "types": "lib/index.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib",
    "lib-es"
  ],
  "scripts": {
    "build": "rm -rf lib &&  rm -rf lib-es && tsc --pretty -p ./tsconfig.build.json && tsc --pretty -p ./tsconfig.build.es.json",
    "test": "jest",
    "clean": "rimraf lib"
  },
  "dependencies": {
    "jaminst-ui-lib": "^0.0.2"
  }
}
